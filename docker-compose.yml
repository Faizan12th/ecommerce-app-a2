version: '3.8'

services:
  backend:
    build:
      context: ./backend
    container_name: ecommerce_ci_backend
    ports:
      - "6000:4000"
    volumes:
      - ./backend:/app

  frontend:
    build:
      context: ./frontend
    container_name: ecommerce_ci_frontend
    ports:
      - "7000:5173"
    volumes:
      - ./frontend:/app
    depends_on:
      - backend
